import React from "react";
import CommentList from "./CommentList";
const CommentComponent = (boardId) => {
  return (
    <>
      <CommentList boardId={1} />
    </>
  );
};

export default CommentComponent;
